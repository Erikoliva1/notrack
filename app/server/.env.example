# Server Configuration
PORT=3000
NODE_ENV=development

# Security - JWT Configuration
# CRITICAL: JWT_SECRET is REQUIRED and must be changed in production!
# Generate a strong secret using: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=24h

# Guest Access Feature Flag
# SECURITY: Set to false in production to enforce JWT authentication
# Set to true only in development to allow connections without authentication
ENABLE_GUEST_ACCESS=false

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=15000
RATE_LIMIT_MAX_REQUESTS=50

# SSL/TLS (for HTTPS)
USE_HTTPS=false
SSL_KEY_PATH=./cert/key.pem
SSL_CERT_PATH=./cert/cert.pem

# Connection Cleanup
CONNECTION_TIMEOUT_MS=1800000
HEARTBEAT_INTERVAL_MS=30000

# ==============================================================================
# ERROR TRACKING & MONITORING (SENTRY)
# ==============================================================================
# Get your Sentry DSN from: https://sentry.io/
# 
# To set up Sentry:
# 1. Create account at https://sentry.io/
# 2. Create a new project (Node.js for server, React for client)
# 3. Copy the DSN from project settings
# 4. Paste it here and in client/.env as VITE_SENTRY_DSN
# 
# Leave empty to disable error tracking
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0

# ==============================================================================
# REDIS CONFIGURATION (OPTIONAL - FOR PERSISTENCE & SCALING)
# ==============================================================================
# Enable Redis for persistent storage and horizontal scaling
# 
# Benefits:
# - Data persists across server restarts
# - Multiple server instances can share state
# - Automatic TTL prevents stale data
# 
# Setup Instructions:
# 1. Install Redis: https://redis.io/download
#    - Windows: https://github.com/microsoftarchive/redis/releases
#    - Mac: brew install redis
#    - Linux: apt-get install redis-server
# 2. Start Redis: redis-server
# 3. Set USE_REDIS=true
# 4. Configure connection details below
#
# For production with Redis Cloud or AWS ElastiCache:
# - Get connection details from your provider
# - Update REDIS_HOST, REDIS_PORT, and REDIS_PASSWORD
USE_REDIS=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
